# Install the missing framer-motion package
npm install framer-motion

# Make sure lucide-react is installed (even though it's in package.json, let's ensure it's installed properly)
npm install lucide-react
